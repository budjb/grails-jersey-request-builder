h1. connectionTimeout

h2. Purpose

Sets the connection timeout of the request, in milliseconds.

h2. Examples

{code}
new RequestBuilder().get {
    uri = "example.com"
    connectionTimeout = 5000 // 5 seconds
}
{code}

h2. Description

Sets the connection timeout of the request, in milliseconds.
