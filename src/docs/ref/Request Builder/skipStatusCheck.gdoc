h1. skipStatusCheck

h2. Purpose

Whether to prevent exceptions for non-successful HTTP status codes.

h2. Examples

{code}
new RequestBuilder().get {
    uri = "http://example.com"
    skipStatusCheck = true
}
{code}

h2. Description

If @true@, prevent the @RequestBuilder@ from throwing an exception if a non-successful HTTP status code is encountered.
