h1. rawClientResponse

h2. Purpose

Instructs the @RequestBuilder@ to return the unmodified "@ClientResponse@":https://jersey.java.net/nonav/apidocs/1.8/jersey/com/sun/jersey/api/client/ClientResponse.html object.

h2. Examples

{code}
new RequestBuilder().post {
    uri = "example.com"
    rawClientResponse = true
}
{code}

h2. Description

If @true@, do not do any response handling, and return the @ClientResponse@ object.
